# === REQUIRED ===
RIOT_API_KEY=replace_me

# Current TFT patch (we only keep this patch; resets when this changes)
PATCH=15.4

# Platforms to pull (comma-separated). We'll merge them into one file.
# You've chosen NA1 + EUW1 + KR.
PLATFORMS=NA1,EUW1,KR

# How deep to scrape per platform per run
SEED_SUMMONERS=1500     # unique summoners per platform (deduped)
MATCHES_PER=20          # 1..20 per PUUID

# Minimum occurrences
MIN_PICKS=200           # min picks to include a comp in ranking
MIN_PICKS_ITEM_COMBO=50 # min picks to include a 3-item combo for a unit

# Run policy
RUN_EVERY_HOURS=6       # informational; cron is set in the workflow
