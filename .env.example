# .env.example
# TEMPLATE ONLY — do not put your real key here.
# For local runs: copy to ".env" and replace RIOT_API_KEY.

RIOT_API_KEY=replace_me

# --- Collector (Diamond+ ranked, append-friendly) ---
PLATFORMS=NA1,EUW1,KR
SEED_SUMMONERS=1500
MATCHES_PER=10
PATCH=
QUEUE_FILTER=1100
MAX_AGE_DAYS=0

# --- Aggregator thresholds ---
MIN_PICKS=100
MIN_PICKS_ITEM_COMBO=30
